// Year 7: Sub-routines and Complete Algorithms
// Loaded by Drills/index.html?drill=y7-flowcharts-subroutines
DrillData.register("y7-flowcharts-subroutines", {
  title: "Year 7: Sub-routines and Complete Algorithms",
  subtitle: "CALL, tracing and reusable flowcharts",
  // [category id, label] in the order the topic picker and mastery overview show them
  categories: [
    ["y7-sub-purpose", "Purpose of Sub-routines"],
    ["y7-call-trace", "Tracing CALL"],
    ["y7-call-effects", "Predicting the Effect"],
    ["y7-complete", "Complete Algorithms"]
  ],
  cards: [
    { id: "y7f3-01", category: "y7-sub-purpose",
      prompt: "What is a sub-routine?",
      answers: ["A named reusable part of an algorithm"],
      keywords: [new RegExp("^(?=.*\\b(named|name)\\b)?(?=.*\\b(reusable|reuse|used again|repeated)\\b)(?=.*\\b(part|section|algorithm|instructions|flowchart)\\b).*$", "i")],
      distractors: ["A random input value","The final arrow only","A decision with no paths","A separate computer"],
      hint: "Named, reusable part of an algorithm",
      note: "A sub-routine is a named group of instructions that can be run whenever the main algorithm calls it." },
    { id: "y7f3-02", category: "y7-sub-purpose",
      prompt: "Why can a sub-routine make a program easier to create and correct?",
      answers: ["Repeated instructions are written and fixed once"],
      keywords: [new RegExp("^(?=.*\\b(write|written|type|typed|fix|fixed|change|changed)\\b)(?=.*\\bonce|one time|single\\b).*$", "i")],
      distractors: ["It removes every decision","It stops all loops","It makes arrows unnecessary","It runs every path together"],
      hint: "Written and fixed once",
      note: "The reusable instructions exist in one place. The main algorithm can call that one copy many times, so a correction only needs to be made once." },
    { id: "y7f3-03", category: "y7-sub-purpose",
      prompt: "A sub-routine is called three times. How many times does its full flowchart need to be written?",
      answers: ["Once"],
      keywords: [new RegExp("^\\s*(once|1|one time)\\s*$", "i")],
      distractors: ["Three times","Twice","Four times","It is never written"],
      note: "The sub-routine is defined once. Each CALL reuses that same definition rather than making another copy." },
    { id: "y7f3-04", category: "y7-sub-purpose",
      prompt: "Does a sub-routine have its own Start and End as a separate flowchart?",
      answers: ["Yes"],
      keywords: [new RegExp("^\\s*(yes|true|it does)\\s*$", "i")],
      distractors: ["No","Only a Start","Only an End","Only when called twice"],
      note: "The lesson treats the sub-routine as its own separate flowchart, complete with its own Start and End symbols." },
    { id: "y7f3-05", category: "y7-sub-purpose",
      prompt: "What allows the main flowchart to refer to a reusable sub-routine without copying all of its instructions?",
      answers: ["The sub-routine name in a CALL block"],
      keywords: [new RegExp("^(?=.*\\bcall\\b)(?=.*\\bname|named|sub.?routine\\b).*$", "i")],
      distractors: ["A second End symbol","A False branch","The output message","A movement distance"],
      hint: "The name in a CALL block",
      note: "A CALL block names the sub-routine to run, such as CALL DrawSquare. The detailed steps stay inside the separate DrawSquare flowchart." },
    { id: "y7f3-06", category: "y7-call-trace",
      prompt: "What happens when execution reaches CALL DrawSquare in Main?",
      answers: ["Execution jumps into the DrawSquare sub-routine"],
      keywords: [new RegExp("^(?=.*\\b(jump|jumps|go|goes|enter|enters|run|runs|start|starts)\\b)(?=.*\\bdrawsquare|sub.?routine\\b).*$", "i")],
      distractors: ["Main immediately ends","A square appears without instructions","The previous line repeats forever","Every sub-routine runs"],
      hint: "Jumps into DrawSquare",
      note: "CALL hands control to the named sub-routine. DrawSquare begins at its own Start and its instructions then run in order." },
    { id: "y7f3-07", category: "y7-call-trace",
      prompt: "Where does execution continue after a called sub-routine reaches its own End?",
      answers: ["At the instruction directly after the CALL in Main"],
      keywords: [new RegExp("^(?=.*\\b(after|next|following)\\b)(?=.*\\bcall\\b)(?=.*\\bmain|instruction|line\\b).*$", "i")],
      distractors: ["At Main Start","At the CALL again","At a random process","At the sub-routine Start"],
      hint: "Right after the CALL in Main",
      note: "Finishing the sub-routine returns control to the calling flowchart. The next instruction is the one directly after that CALL." },
    { id: "y7f3-08", category: "y7-call-trace",
      prompt: "After a sub-routine finishes, does execution return to Main own Start?",
      answers: ["No"],
      keywords: [new RegExp("^\\s*(no|false|it does not|it doesn.t)\\s*$", "i")],
      distractors: ["Yes","Only after two calls","Only for movement","Only when there is no End"],
      note: "Control does not restart Main. It resumes directly after the CALL so the remaining main instructions can continue." },
    { id: "y7f3-09", category: "y7-call-trace",
      prompt: "Main contains CALL Flash followed later by another CALL Flash. How many times does Flash run?",
      answers: ["Twice"],
      keywords: [new RegExp("^\\s*(twice|2|two times)\\s*$", "i")],
      distractors: ["Once","Three times","Zero times","Forever"],
      note: "Each CALL runs the named sub-routine once. Two CALL blocks therefore cause two complete runs of Flash." },
    { id: "y7f3-10", category: "y7-call-trace",
      prompt: "Sub-routine Beep says \"Beep\", waits, then says \"Boop\". Main calls Beep twice and then ends. What is the very last thing said?",
      answers: ["Boop"],
      keywords: [new RegExp("^\\s*[\"']?boop[\"']?\\s*$", "i")],
      distractors: ["Beep","Wait","Done","Nothing"],
      note: "The second CALL repeats the complete Beep sub-routine. Its final output is \"Boop\", so that is also the last output overall." },
    { id: "y7f3-11", category: "y7-call-effects",
      prompt: "Main is Start -> CALL DrawSquare -> End. What is drawn?",
      answers: ["One square"],
      keywords: [new RegExp("^\\s*(one|1|a)\\s+square\\s*$", "i")],
      distractors: ["Two squares","Nothing","A triangle","Four separate lines only"],
      note: "One CALL runs DrawSquare once. Its four equal move-and-turn pairs complete one square." },
    { id: "y7f3-12", category: "y7-call-effects",
      prompt: "Main is Start -> CALL DrawSquare -> Turn right 90 -> CALL DrawSquare -> End. Describe the result.",
      answers: ["Two squares rotated 90 degrees apart"],
      keywords: [new RegExp("^(?=.*\\b(two|2)\\b)(?=.*\\bsquares?\\b)(?=.*\\b90|rotated|turn\\b).*$", "i")],
      distractors: ["One square","Two squares in the same place","No shape","A single straight line"],
      hint: "Two squares, 90 degrees apart",
      note: "The first CALL draws one square. Main turns 90 degrees, then the second CALL draws the same square routine from the new direction." },
    { id: "y7f3-13", category: "y7-call-effects",
      prompt: "Main calls DrawSquare twice with no turn or movement between the two CALLs. Where are the squares drawn?",
      answers: ["In exactly the same place"],
      keywords: [new RegExp("^(?=.*\\b(same|exact)\\b)(?=.*\\b(place|position|location|overlap|top)\\b).*$", "i")],
      distractors: ["90 degrees apart","On opposite sides","Only the second appears","At random positions"],
      note: "Nothing changes the sprite position or direction between the calls. The second square therefore follows the same path as the first and overlaps it." },
    { id: "y7f3-14", category: "y7-call-effects",
      prompt: "Main is Start -> End, and never calls DrawSquare. How many squares does DrawSquare produce?",
      answers: ["None"],
      keywords: [new RegExp("^\\s*(none|zero|0|no squares?)\\s*$", "i")],
      distractors: ["One","Two","Four","Infinitely many"],
      note: "A sub-routine only runs when something calls it. Merely defining DrawSquare does not make its instructions execute." },
    { id: "y7f3-15", category: "y7-call-effects",
      prompt: "The Beep sub-routine is written once. Main contains CALL Beep twice. How many times is the word Beep written out in full in the program definition?",
      answers: ["Once"],
      keywords: [new RegExp("^\\s*(once|1|one time)\\s*$", "i")],
      distractors: ["Twice","Three times","Zero times","Four times"],
      note: "The instructions are stored in one sub-routine definition. The two CALL blocks reuse that single copy." },
    { id: "y7f3-16", category: "y7-complete",
      prompt: "Blink should turn a light on, wait, then turn it off. It currently says: 1 Start, 2 Light on, 3 Light on, 4 Wait, 5 End. Which numbered step is wrong?",
      answers: ["Step 3"],
      keywords: [new RegExp("^\\s*(step\\s*)?3\\s*$", "i")],
      distractors: ["Step 1","Step 2","Step 4","Step 5"],
      note: "Step 3 repeats Light on instead of completing the required on, wait, off behaviour. It is the instruction that must be corrected." },
    { id: "y7f3-17", category: "y7-complete",
      prompt: "In the faulty Blink sub-routine, step 3 says Light on for a second time. What should step 3 say instead?",
      answers: ["Light off"],
      keywords: [new RegExp("^\\s*(turn\\s+)?light\\s+off\\s*$|^\\s*off\\s*$", "i")],
      distractors: ["Light on","Wait","CALL Blink","End"],
      note: "The required result includes turning the light off. Replacing the repeated Light on instruction with Light off supplies that missing action." },
    { id: "y7f3-18", category: "y7-complete",
      prompt: "Flash is Start -> Light on -> Light off -> End. Main is Start -> CALL Flash -> Say \"Done\" -> End. What runs immediately after Flash finishes?",
      answers: ["Say \"Done\""],
      keywords: [new RegExp("\\bdone\\b", "i")],
      distractors: ["Main Start","Light on","CALL Flash again","Nothing"],
      note: "Flash returns control to the point after CALL Flash. The next Main instruction is Say \"Done\"." },
    { id: "y7f3-19", category: "y7-complete",
      prompt: "Does a CALL block contain all of the sub-routine instructions itself? Explain briefly.",
      answers: ["No, it names the sub-routine and hands control to its separate flowchart"],
      // "names" (present-tense verb, as in the card's own hint "it just
      // names it") was missing - only "name"/"named" matched as whole
      // words, so the hint's own wording failed this check.
      keywords: [new RegExp("^(?=.*\\b(no|does not|doesn.t)\\b)(?=.*\\b(name|names|named|jump|jumps|control|separate)\\b).*$", "i")],
      distractors: ["Yes, it copies every instruction","Yes, but only for loops","No, it ends the whole program","No, it deletes the sub-routine"],
      hint: "No - it just names it",
      note: "The CALL block is a reference to the named reusable flowchart. The actual instructions remain in the sub-routine definition." },
    { id: "y7f3-20", category: "y7-complete",
      prompt: "Sub-routine MoveRight is Point in direction 90 -> Move 10 steps. Main calls MoveRight twice. How far and in which direction does the sprite move in total?",
      answers: ["20 steps to the right"],
      keywords: [new RegExp("^(?=.*\\b20\\b)(?=.*\\bright\\b).*$", "i")],
      distractors: ["10 steps right","20 steps left","90 steps right","The sprite does not move"],
      note: "Each CALL points right and moves 10 steps. Running the sub-routine twice gives 10 + 10 = 20 steps to the right." }
  ]
}
/* YEAR 7 REVISION DRILLS END */);
